import React from 'react';
import PropTypes from 'prop-types';

WebFeature.propTypes = {
    
};

function WebFeature(props) {
    return (
        <div>
            
        </div>
    );
}

export default WebFeature;