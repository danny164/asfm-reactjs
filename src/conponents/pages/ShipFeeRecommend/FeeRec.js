import React from 'react';
import PropTypes from 'prop-types';

FeeRec.propTypes = {
    km: PropTypes.number
};

FeeRec.defaultProps = {
    km: 0
}
function FeeRec(props) {
    

    return (
        <div>
            
        </div>
    );
}

export default FeeRec;